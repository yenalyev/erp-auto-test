# ===========================================
# Local Environment — Testcontainers
# Run: mvn test -Denv=local -Duse.docker=true -Dsuite=functional
# Requires: Docker Desktop running
# ===========================================

# URL is overridden by TestcontainersManager at runtime
base.url=http://localhost:8080

# Test user credentials (must exist in Keycloak container after realm import)
auth.username=admin
auth.password=karta33#

# Keycloak — URL is overridden by TestcontainersManager at runtime
auth.keycloak.url=http://localhost:8180
auth.keycloak.realm=tk
auth.keycloak.client.id=tk-admin
auth.keycloak.client.secret=aI8sUo3W6kRvEzCy9cLY9unvSjwgdgks

# Database — Testcontainers provides PostgreSQL
use.database=true
db.url=jdbc:postgresql://localhost:5432/erp_test
db.username=test
db.password=test

# Logging
logging.verbose=true

# API Timeout
api.timeout=30
