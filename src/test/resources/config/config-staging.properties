# ===========================================
# Staging Environment
# ???????????????? ??? ??????????
# ?? ??????????? staging ???????
# ===========================================

# Application URL
base.url=https://staging.erp-system.com

# Authentication (???????????? environment variables!)
auth.username=${STAGING_USER:staging-user}
auth.password=${STAGING_PASSWORD:changeme}
auth.keycloak.url=https://auth.staging.erp-system.com
auth.keycloak.realm=erp-realm
auth.keycloak.client.id=erp-client
auth.keycloak.client.secret=${KEYCLOAK_CLIENT_SECRET:}

# Database (read-only ??? ??????????)
use.database=false
db.url=jdbc:postgresql://staging-db.erp-system.com:5432/erp_staging
db.username=readonly_user
db.password=${DB_PASSWORD:changeme}

# Logging (???? ???????? ??? staging)
logging.verbose=false

# API Configuration
api.timeout=60