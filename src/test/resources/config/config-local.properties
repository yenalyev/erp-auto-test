# ===========================================
# Local Testing Environment (? Testcontainers)
# ???????????? ???? ????? ?????????
# PostgreSQL ? Keycloak ? Docker ???????????
# ???????? ????????? Docker Desktop!
# ===========================================

# Application URL (???? ????????????? Testcontainers)
base.url=http://localhost:8080

# Authentication (???? ? Testcontainers)
auth.username=test-user
auth.password=test-password
auth.keycloak.url=http://localhost:8180
auth.keycloak.realm=erp-realm
auth.keycloak.client.id=erp-client
auth.keycloak.client.secret=

# Database (Testcontainers ???????? ???????????)
use.database=true
db.name=erp_test
db.username=test
db.password=test

# Testcontainers Settings
testcontainers.reuse.enable=true

# Logging
logging.verbose=true

# API Configuration
api.timeout=30