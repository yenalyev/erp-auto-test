{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RelocationResponseList",
  "description": "Schema for a list of relocations (must contain at least one item)",
  "type": "array",
  "minItems": 1,
  "items": {
    "$ref": "relocation-response-schema.json"
  }
}