package com.erp.enums;

/** Stable keys for dynamically created test-location templates. */
public enum LocationProfile {
    BATTALION_UNIT,
    BATTALION_WARENHAUSE_UNIT,
    TSUK_WARENHAUSE,
    TSUK_PRODUCTION
}
